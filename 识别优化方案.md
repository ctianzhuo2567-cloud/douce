# 豆册图纸识别优化方案

## 1. 结论

下一版不应继续只调高整图 OCR 的分辨率。拼豆图纸具有固定网格、有限色号字典、重复色块和图例统计，这些结构信息比通用 OCR 更可靠。

建议将识别流程改为：

> 页面清理 → 图纸与图例分区 → 网格定位 → 单格切片 → 多路识别 → MARD221 约束 → 图例与格子交叉校验 → 低可信项集中人工修正

第一阶段继续完全离线运行，不上传用户图片。

## 2. 当前方案的主要问题

1. 对整张大图直接 OCR，小色号在缩放后可能只剩几个像素。
2. 当前网格位置依赖手动裁剪和行列数，不是真正检测网格线。
3. 图例中多个色号和数量可能被 OCR 合并为同一行，造成数量配错。
4. 只按文字格式生成所谓置信度，不能反映真实识别可靠性。
5. 一个色号完全漏识别后，现有网格编辑器无法补充该色号。
6. 水印、截图黑边、状态栏、压缩噪声和浅色文字会干扰整图 OCR。

## 3. 推荐识别架构

### 3.1 第一步：页面清理和类型判断

- 自动去除手机截图中的黑边、状态栏和查看器空白区域。
- 检测纸张主体，校正轻微旋转和透视变形。
- 判断图纸属于小图、密集大图还是多作品合集。
- 自动寻找图纸区和底部图例区；无法确定时让用户拖动两个裁剪框确认。

这一步只做结构定位，不立即识别色号。

### 3.2 第二步：检测真实网格

同时使用三种证据生成网格候选：

1. 横线、竖线的亮度投影峰值。
2. 线段检测得到的等间距交点。
3. 色块边界的周期性。

系统给出自动推断的行列数和边框。用户确认后再进入 OCR。不要在网格位置不确定时直接输出完整结果。

粗网格、每 5 或 10 格的加粗线只能作为辅助证据，最终格子边界应由全部细网格共同决定。

### 3.3 第三步：先切格，再识别

不要把整张图缩到固定上限后一次识别。应先按原图坐标裁出网格，再将每个格子缩放到统一目标尺寸，例如 48～64 像素。

为了兼顾速度，可按 10×10 或 20×20 格组成小图块批量识别，并保留一格重叠区域。每个图块分别生成：

- 原色版本
- 灰度增强版本
- 自适应二值化版本
- 反色版本，仅用于深色背景白字

同一格从多种版本得到多个候选，只有候选一致或通过后续校验时才判为高可信。

### 3.4 第四步：使用 MARD221 有限字典纠错

识别结果必须限制为 MARD221 合法色号，而不是接受任意文本。

典型流程：

1. 统一大写并清理空格、括号和标点。
2. 根据 MARD221 字典修正常见混淆，如 `0/O`、`1/I/L`、`5/S`、`8/B`。
3. 根据图例中已经出现的色号缩小格子候选范围。
4. 对不在图例中的格子降低可信度，但不要直接删除，以免图例本身漏识别。

MARD 色号结构有限，长期可以训练一个只识别 `A-H、M + 数字` 的小型离线模型。它通常会比通用文字 OCR 更适合密集图纸。

### 3.5 第五步：利用颜色聚类补齐漏识别

这里的颜色只作为同类传播依据，不用于把颜色直接转换成 MARD 色号。

例如某批外观近似的深红格子中，20 个格子有 17 个稳定识别为 F7，另外 3 个文字模糊，可以将 F7 作为这 3 格的候选，并标记为“推测”，交给用户一次确认整组。

聚类特征可包含：

- 去除文字区域后的格子中位颜色
- 明暗和饱和度
- 字符轮廓相似度
- 相邻格子的重复模式

这种方式可以显著减少逐格 OCR 的漏识别，但不能自动把低可信结果伪装成确定结果。

### 3.6 第六步：图例与网格双向校验

图例应按单个色卡或单个圆形色号分别裁剪，再识别色号和数量，避免整行串位。

识别完成后进行以下核对：

- 图例色号是否都在网格中出现。
- 网格统计数量是否与图例数量一致。
- 网格中是否存在图例没有列出的色号。
- 同一色号是否被拆成两个易混淆候选。
- 所有格子数量是否符合图纸标注的总颗数。

数量一致是强证据；数量不一致时应显示差额，而不是给出看似精确的百分比。

## 4. 真实可信度设计

删除目前按格式生成的“92%”一类数值。每个结果记录具体证据：

- `ocrAgreement`：多种预处理结果是否一致。
- `dictionaryValid`：是否为合法 MARD221 色号。
- `legendPresent`：是否在图例中出现。
- `countConsistent`：统计数量是否与图例一致。
- `clusterAgreement`：同类色块是否支持该结果。
- `gridFit`：文字中心是否位于正确格子范围。

界面只显示：

- 高可信：多路一致且统计吻合。
- 待确认：只有部分证据支持。
- 冲突：图例、格子或数量之间存在矛盾。

每个判断必须能够展开查看原因。

## 5. 人工纠错界面

识别无法做到百分之百，因此重点不是隐藏错误，而是让用户快速修正一组错误。

下一版整理页应支持：

- 修改一个格子的色号。
- 按视觉相似分组批量修改。
- 添加漏掉的任意 MARD221 色号。
- 擦除空白格或水印误识别格。
- 撤销和重做。
- 长按查看对应原图局部。
- 只浏览“待确认”和“冲突”格子。
- 修改后实时重新统计图例和总颗数。

确认后的修改应记录为纠错数据，后续可以用于优化规则或训练专用模型，但默认只保存在本机。

## 6. 安卓端性能方案

- 使用后台任务执行 OCR，切换页面后可以继续，支持中断恢复。
- 原图按区域解码，避免整张超大图片同时进入内存。
- 网格数据改用 Room 或紧凑数组保存，不再把全部格子塞进一段 SharedPreferences JSON。
- 网格画布只绘制屏幕可见区域，并缓存色块层和文字层。
- OCR 图块完成后立即落盘，应用被系统回收后能够继续。
- 为低内存设备提供“标准识别”和“精细识别”两档；精细模式使用更多预处理版本但耗时更长。

## 7. 分版本实施

### v0.5.1：先减少明显误识别

1. 图例分卡裁剪和多尺度 OCR。
2. 原图区域先裁剪后放大，不再盲目缩小整图。
3. MARD221 字典约束和常见字符混淆纠正。
4. 识别结果支持修改、删除、补充和重新统计。
5. 删除虚假的百分比置信度。
6. 为识别解析、坐标映射和备份恢复补充自动测试。

### v0.6.0：实现结构化网格识别

1. 自动检测网格边框、行列和倾斜。
2. 单格切片和图块 OCR。
3. 图例数量与网格数量交叉校验。
4. 相似色块分组和批量纠错。
5. 镜像、编辑、高亮都基于同一份独立网格数据。

### v0.7.0：专用离线识别模型

1. 用常见像素字体、压缩、模糊、水印、浅色和深色背景合成训练数据。
2. 训练仅识别 MARD 色号字符的小型模型。
3. 使用用户主动确认的匿名纠错样本做离线评估；未经同意不上传图片或数据。

## 8. 测试和验收标准

现有 7 张测试图不能只用于演示，应建立人工标注的标准答案，包括：

- 网格边界和行列数。
- 图例色号及数量。
- 每个非空格子的标准色号。
- 水印、黑边和非图纸区域。

每次发布前自动输出以下指标：

1. 网格行列识别正确率。
2. 图例色号准确率和召回率。
3. 非空格识别准确率和召回率。
4. 数量完全一致的色号比例。
5. 需要用户确认的格子数量。
6. 处理时间、峰值内存和网格编辑帧率。

v0.6.0 建议最低验收目标：

- 7 张测试图均能正确确定或经一次确认确定网格范围和行列。
- 图例色号准确率不低于 98%，召回率不低于 95%。
- 清晰小图的非空格准确率不低于 97%。
- 密集大图经低可信项确认后，最终非空格准确率不低于 99%。
- 94×87 图纸在主流中端安卓机上识别时间控制在 20 秒以内。
- 识别失败时保留原图和中间结果，不产生不可恢复的数据覆盖。

这些指标只在建立人工标准答案后才有意义，不能用“看起来差不多”代替。

## 9. 下一次开发的首个实验

先不要一次性实现全部功能。使用 2.jpg（16×16 清晰小图）、4.jpg（密集大图）和 6.jpg（87×94 超大图）做三个难度档位，完成以下最小实验：

1. 手动确定网格范围和行列，排除自动找网格造成的干扰。
2. 将格子统一放大至 64×64。
3. 对原色、灰度、自适应二值化和反色四种版本识别。
4. 限制输出为图例中出现的 MARD221 色号。
5. 比较整图 OCR 与分格多路 OCR 的准确率、耗时和内存。
6. 如果分格方案明显胜出，再开发自动网格检测；如果没有胜出，先训练专用字符模型。

这个实验能以较小开发成本验证核心方向，避免先投入复杂 UI 和网格检测后才发现底层 OCR 不适合。
